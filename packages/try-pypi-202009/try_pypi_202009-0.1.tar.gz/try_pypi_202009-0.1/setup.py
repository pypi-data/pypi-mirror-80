from setuptools import setup

setup(name='try_pypi_202009',
      version='0.1',
      description='Try upload a package to pypi',
      packages=['try_pypi_202009'],
      author = 'udacity',
      zip_safe=False)

from setuptools import setup

setup(name='write_patch',
      version='0.1',
      description='This is the write patch module for the turtle module, so turtle can write text with angle argument.配合Python海龟画图写字补丁模块，这样就能让海龟写斜着的字了',
      url='https://github.com/lixingqiu/write_patch',
      author='lixingqiu',
      author_email='406273900@qq.com',
      license='MIT',
      packages=['write_patch'],
      zip_safe=False)


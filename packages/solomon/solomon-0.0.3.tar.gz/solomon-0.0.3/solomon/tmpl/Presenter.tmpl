import Foundation

class ${name}Presenter {
    
    private let model: I${name}Model
    private weak var view: I${name}View?
    
    init(_ model: I${name}Model, _ view: I${name}View) {
        self.view = view
        self.model = model
    }

    // TODO: need implement presenter for view use
    
}

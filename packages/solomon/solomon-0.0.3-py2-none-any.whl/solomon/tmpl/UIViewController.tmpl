import Foundation
import UIKit
import SnapKit

class ${name}UIViewController: ViewController<${name}Presenter> {
    
    override func viewDidLoad() {
    }
}

extension ${name}UIViewController: I${name}View {
}

#!/bin/bash
current_dir="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
declare -a targets
targets=$(echo "${current_dir}"/../lib/python*/site-packages/bin/cctbx.show_systematic_absences)
exec "${targets[0]}" "$@"

from setuptools import setup

setup(name='expsolver',
      version='0.3',
      description='Expression solver',
      packages=['expsolver'],
      long_description = 'README.md',
      zip_safe=False)
[tool.poetry]
name = "poetry-demo-jerber-new"
version = "0.1.1"
description = ""
authors = ["Jeremy Berman <jerber@sas.upenn.edu>"]

[tool.poetry.dependencies]
python = "^3.8"
pydantic = "^1.6.1"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
